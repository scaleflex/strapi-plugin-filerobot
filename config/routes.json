{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "scaleflex-filerobot.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/config",
      "handler": "scaleflex-filerobot.getConfig",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/update-config",
      "handler": "scaleflex-filerobot.updateConfig",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/media",
      "handler": "scaleflex-filerobot.getMedia",
      "config": {
        "policies": []
      }
    }
  ]
}
